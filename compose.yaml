services:
  default:
    image: python:3.11-slim
    command: >
      bash -c "pip install astropy numpy pytest -q && tail -f /dev/null"
    working_dir: /workspace
    volumes:
      - ./rag/repos/astropy:/workspace/astropy
    environment:
      - PYTHONPATH=/workspace