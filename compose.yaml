services:
  default:
    image: python:3.11-slim
    command: tail -f /dev/null
    working_dir: /workspace
    volumes:
      - ./rag/repos/astropy:/workspace/astropy
    environment:
      - PYTHONPATH=/workspace