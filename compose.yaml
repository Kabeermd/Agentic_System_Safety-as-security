services:
  default:
    build: .
    command: tail -f /dev/null
    working_dir: /testbed
    volumes:
      - D:/dissertation_repos:/testbed
    environment:
      - PYTHONPATH=/testbed
    healthcheck:
      test: ["CMD", "python3", "--version"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s